@@ -62,7 +62,7 @@
         # Build the indicator's dataframe
         ic = pd.DataFrame(
             index=self._input_data.index,
-            columns=['tenkan_sen', 'kijun_sen', 'senkou_a', 'senkou_b'],
+            columns=['tenkan_sen', 'kijun_sen', 'senkou_a', 'senkou_b', 'chikou_span'],
             data=None)
 
         ic['tenkan_sen'] = (self._input_data['high'].
@@ -83,6 +83,8 @@
                            rolling(window=52, min_periods=1).min()) / 2).\
             shift(26)
 
+        ic['chikou_span'] = self._input_data['close'].shift(-26)
+
         return ic.round(4)
 
     @staticmethod
